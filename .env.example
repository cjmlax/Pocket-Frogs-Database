# Base URL of the pfdb-submissions worker API.
# Leave EMPTY for same-origin: the site proxies /api/* to the worker, which
# avoids CORS entirely. This is the default. Only set an absolute URL for local
# dev against a deployed worker, e.g. https://pfdb.cjmlax.com
VITE_SUBMIT_API=
